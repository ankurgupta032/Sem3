/*4-Write a Program that accepts two Strings as command line arguments and generate the output in the required format.
Example1)
If the two command line arguments are GLA and Mathura then the output generated should be GLA University Mathura.*/

import java.util.*;
class test1 {
        public static void main(String args[])
        {
            String gla=args[0];
            String mathura=args[1];
            System.out.println(gla+" "+"University"+" "+mathura);
        }
    }
